# Build context for services/export-worker/Dockerfile is the REPO ROOT. Keep
# host node_modules (wrong-platform binaries that would clobber the Linux
# install) and any secrets out of the image.
**/node_modules
**/.next
.git
**/*.log
**/.env
**/.env.*
!**/.env*.example
services/export-worker/dist
# .NET build artifacts (services/export-api-dotnet) — rebuilt inside the image.
**/bin
**/obj
.claude
.vscode
