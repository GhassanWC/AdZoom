steps:
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'build'
      - '-t'
      - '${_IMAGE}'
      - '-f'
      - 'services/export-api-dotnet/Dockerfile'
      - '.'

images:
  - '${_IMAGE}'