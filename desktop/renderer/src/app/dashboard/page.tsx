import { DashboardHome } from "@/components/dashboard/DashboardHome";

/** `/dashboard` — the website's own home screen, unchanged. */
export default function DesktopDashboardPage() {
  return <DashboardHome />;
}
